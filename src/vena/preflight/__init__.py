"""Pre-flight (gating) checks for the VENA pipeline.

Each sub-package corresponds to one routine under ``routines/preflights/`` and
holds the library implementation that the routine's thin engine orchestrates.
See ``.claude/rules/preflight-pattern.md`` for the layout contract.
"""
