"""BraTS-Africa (SSA) H5 cache producers (image- and latent-domain).

Two pathology subsets share a common converter and manifest; each subset is
selected at runtime via the ``cohort_name`` config field.
"""
