# BraTS-GLI image-domain H5 cache — smoke run (schema 2.0.0).
#
# 8 patients (n_jobs=2, n_test_min=2, 3-fold CV): sanity-checks the converter
# (LAS→LPS reorient, brain mask from t1n foreground, crop/origin, CSR grouping,
# longitudinal patient contiguity, nested CV splits) without the full 1251-session
# sweep. Overwrites output so reruns are clean.

source_root: /media/mpascual/MeningD2/GLIOMA/BRATS_GLI/PRE_OPERATIVE/BraTS2025-GLI-PRE-Challenge-TrainingData
output_path: /tmp/vena_smoke_h5/BraTS_GLI_image_smoke.h5

n_jobs: 2
crop_box: [192, 224, 192]
test_fraction: 0.10
n_test_min: 2
n_folds: 3
seed: 0

overwrite: true
limit: 8
log_level: INFO
